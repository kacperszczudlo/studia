import { TextFormat } from './text-format';

describe('TextFormat', () => {
  it('should create an instance', () => {
    const directive = new TextFormat();
    expect(directive).toBeTruthy();
  });
});
