#include <stdio.h>

    int main()
    {
        char a, b, c, d;
        scanf(" %c %c %c %c",&a,&b,&c,&d);
        fflush(stdin);
        printf("Tekst: %c, %c %c %c \n",a,b,c,d);
        return 0;
    }
